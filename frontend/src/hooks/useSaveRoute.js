import { useRouter } from "next/router";
import { useState } from "react";

export const userSavedRoutes = () => {
    const saveroute = async (user,savedRoutes) => {
        /* post savedRoutes into db */

}
}